services:
  web:
    build: .
    image: web
    container_name: web
    restart: unless-stopped
    ports:
      - '127.0.0.1:3000:3000'
    environment:
      - NODE_ENV=production
    logging:
      driver: 'json-file'
      options:
        max-file: '5'
        max-size: '10m'
